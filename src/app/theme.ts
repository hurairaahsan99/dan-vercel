import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    body: "'ReadexPro'",
    heading: "'ReadexPro', sans-serif",
    link: "'ReadexPro', sans-serif",
  },
});

export default theme;
